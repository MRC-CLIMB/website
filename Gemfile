source "https://rubygems.org"

# Deploy via GitHub Pages (Actions) — this pin matches what GitHub's build
# servers run, so a site that builds locally builds on Pages unchanged.
gem "github-pages", group: :jekyll_plugins

# Needed for local `jekyll serve` on Ruby 3+ and for any local build.
gem "webrick", "~> 1.8"